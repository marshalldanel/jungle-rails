<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Hello, here are your recent order details:</h1>
    
    <h2>You purchased:</h2>
    <ul>
      <% @order.line_items.each do |line_item| %>
        <li>
          <h4>Item: <%= line_item.product.name %></h4>
          <p>Quanity: <%= line_item.quantity %></p>
          <p>Price per item: <%= line_item.item_price %></p>
        </li>
      <% end %>
    </ul>

    <h2>Order Total: <%= number_to_currency(@order.total) %></h2>
  </body>
</html>